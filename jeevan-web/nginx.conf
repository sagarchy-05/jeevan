server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: serve index.html for client-side routes (/doctors, /appointments, /verify, …)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
